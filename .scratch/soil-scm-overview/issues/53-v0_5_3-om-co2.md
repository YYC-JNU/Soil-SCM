# 53 — v0.5.3 OM 矿化产 CO₂（T4）

**What to build:** `climate_forcing.py` 新增 `apply_om_pco2(pco2_base, om_gkg, k_om, pco2_max)`：加性 pCO₂_eff = base(T) + k_om×OM_i，钳制 ≤ pCO₂_max；`constants.py` 新增 `k_om=0.0005`、`pco2_max=0.05`（atm）、`OM_PROFILE_4LAYER=[30,15,8,5]`（g/kg）；4 层内置默认注入 OM 垂直剖面（layer_overrides 可逐层覆盖）；逐层有效 pCO₂ = base(T) + k_om×OM_i（温度独立，T 响应仅归 base 项）注入 GAS_PHASE；输出新增 `pCO2_eff` 诊断列。

**Blocked by:** 50（与 51/52 并行）

**Status:** ✅ 已完成 (2026-08-19, v0.5.3)

- [x] 加性公式 + 钳制：高 OM 下 pCO₂_eff ≤ pCO₂_max（不失控）
- [x] 温度独立性：同 OM、异 T 时 ΔpCO₂ 恒定（S3 专家★3）
- [x] 垂直梯度：OM [30,15,8,5] → pCO2_eff L1≥L2≥L3≥L4 且表层增量最大（S3 专家★3）
- [x] 4 层内置默认 OM 剖面生效（无 layer_overrides 覆盖时）
- [x] pCO2_eff 诊断列输出 → **随工单 54（输出集成）落地**（逐层 layer_pco2s 已含 OM 调制, 诊断列在输出层扩展）
- [x] 测试（S3 + S4 + S6）：全绿；**232 passed 全绿**
