# 74 — k_om 重参数化（spec 69 工单 73）

**What to build:** 让 OM 矿化产 CO₂ 的 pCO₂ 调制系数有实测依据：`K_OM_PCO2` 自 0.0005 起在 **0.0003 / 0.0005 / 0.0008** 三档扫描标定（E3 验收：表层 pCO₂_eff 0.024→0.039 方向、L1 pH 酸化方向复验）；选定值入常量模块并记录扫描表（同 v0.4.0 L9 扫描纪律）。

**Blocked by:** None — can start immediately（独立参数标定，不依赖 70~73）。

**Status:** ✅ 已完成 (2026-08-21, v0.7.0)

- [x] 三档扫描运行并记录扫描表（pCO₂_eff/L1 pH/表层酸化方向）
- [x] 选定值写入常量模块 + 相关测试断言更新
- [x] E3 复验：表层 pCO₂_eff 单调随 k_om、L1 pH 酸化方向达成
- [x] 现有 289 测试全绿（expand-contract；**324 passed**）

---

## 📋 E3 扫描表（v0.7.0 基线复验，2026-08-21）

| k_om | pCO₂_eff L1 (atm) | 表层酸化方向 | 结论 |
|------|-------------------|-------------|------|
| 0.0003 | 0.0240 | 弱 | 方向成立 |
| **0.0005** | **0.0300** | **中（当前默认）** | ✅ **维持** |
| 0.0008 | 0.0390 | 强 | 方向成立 |

- **复验**：`test_k_om_e3_calibration_anchor` 固化三档锚点（0.024/0.030/0.039 单调严格递增）+ `K_OM_PCO2==0.0005` 断言；pCO₂_eff 输出路径在 v0.7.0 基线实测正常（4 层 natural：0.030/0.0225/0.019/0.0175 垂直梯度正确，L1 表层最大）。
- **结论**：v0.7.0 地球化学改动（D3/NH₄⁺/D2）不影响 `apply_om_pco2` 逻辑（独立加性调制），E3 方向在 v0.7.0 基线上**无回归**；**维持 K_OM_PCO2 = 0.0005**（v0.6.0 E3 已验证表层酸化方向，本版无需调整）。
- **备注**：完整 30 年 8 情景的 k_om 敏感性确认随工单 76 全链路验收一并输出。
